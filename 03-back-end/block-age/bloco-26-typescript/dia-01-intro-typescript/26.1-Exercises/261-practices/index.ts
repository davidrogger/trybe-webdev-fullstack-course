enum StudantStatus {
  Active,
  Inactive,
  Paused
};

let newStudentStatus: StudantStatus = StudantStatus.Inactive;

console.log(newStudentStatus);

enum rainbowColors {
  red = "red",
  orange = "orange",
  yellow = "yellow",
  green = "green",
  blue = "blue",
  indigo = "indigo",
  violet = "violet"
};

enum files {
  save,
  print,
  open,
  view,
  close
};

enum cardealPoints {
  North = "N",
  East = "E",
  South = "S",
  West = "W"
};
